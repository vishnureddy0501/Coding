	// conunting sort is helpful if the Maximum element in array is low.
	// count elements and insert into new array based on count.
	
	vector<int> freq(101, 0), expect;
	for (int i = 0; i<nums.size(); i++) {
		freq[nums[i]]++;
	}
	for (int i = 0; i<freq.size(); i++) {
		int val = freq[i];
		while(val--) expect.push_back(i);
	}